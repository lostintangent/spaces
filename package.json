{
	"name": "vsls-can",
	"displayName": "vsls-can",
	"description": "",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.36.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"*"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "vsls-can.joinNetwork",
				"category": "Live Share CAN",
				"title": "Join Network"
			},
			{
				"command": "vsls-can.leaveNetwork",
				"category": "Live Share CAN",
				"title": "Leave Network"
			},
			{
				"command": "vsls-can.refresh",
				"category": "Live Share CAN",
				"title": "Refresh"
			}
		],
		"views": {
			"liveshare": [
				{
					"id": "vsls-can.contacts",
					"name": "Collaboration Area Network"
				}
			]
		},
		"configuration": {
			"type": "object",
			"title": "Live Share CAN",
			"properties": {
				"vsls-can.showSuggestedContacts": {
					"default": true,
					"type": "boolean",
					"description": "Specifies whether to show CAN-discovered contacts in the Live Share Suggested Contacts tree."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^10.12.21",
		"@types/vscode": "^1.36.0",
		"glob": "^7.1.4",
		"mocha": "^6.1.4",
		"typescript": "^3.3.1",
		"tslint": "^5.12.1",
		"vscode-test": "^1.0.0-next.0"
	},
	"dependencies": {
		"redux": "^4.0.4",
		"redux-thunk": "^2.3.0",
		"vsls": "^0.3.1291"
	}
}
